# LLM / 대화 (Gemini Live API 주 경로, batch+TTS 폴백 — docs/architecture.md §03)
# google-genai = Live API(client.aio.live.connect)에 필요한 신규 통합 SDK.
# google-generativeai = batch 폴백 경로(genai.GenerativeModel)용 구 SDK. 둘 다 필요.
google-genai
google-generativeai

# 얼굴 추적 / 인식
opencv-python
mediapipe
insightface
onnxruntime  # GPU 사용 시 onnxruntime-gpu로 교체

# 모터 제어
dynamixel-sdk
pyserial

# 오디오 입출력
sounddevice
numpy
scipy  # media/audio_manager.py의 AEC 원단 리샘플링(24kHz->16kHz)에 사용
aec-audio-processing  # 에코캔슬레이션(WebRTC AEC3 바인딩) — media/audio_manager.py
pyworld  # 목소리 피치/포먼트 시프트(WORLD 보코더) — media/voice_shift.py

# 표정 UI
pygame

# 입력 / 설정
pynput
python-dotenv
screeninfo
